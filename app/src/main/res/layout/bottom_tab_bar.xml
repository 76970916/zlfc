<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:orientation="horizontal">

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:checkedButton="@+id/rbHome"
        android:orientation="horizontal"
        android:padding="6dp">

        <RadioButton
            android:id="@+id/tab_rb_home"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:checked="true"
            android:drawableTop="@drawable/btn_tabbar_home_selector"
            android:drawablePadding="4dp"
            android:gravity="center"
            android:text="@string/tab_home"
            android:textColor="@color/tab_text_color"
            android:textSize="@dimen/bottom_menu_text_size" />

        <RadioButton
            android:id="@+id/tab_rb_design"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:checked="false"
            android:drawableTop="@drawable/btn_tabbar_design_selector"
            android:drawablePadding="4dp"
            android:gravity="center"
            android:text="@string/tab_design"
            android:textColor="@color/tab_text_color"
            android:textSize="@dimen/bottom_menu_text_size" />


        <RadioButton
            android:id="@+id/tab_rb_me"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/btn_tabbar_more_selector"
            android:drawablePadding="4dp"
            android:gravity="center"
            android:text="@string/tab_more"
            android:textColor="@color/tab_text_color"
            android:textSize="@dimen/bottom_menu_text_size" />

    </RadioGroup>
</LinearLayout>